<section class="iwt color-{{ section.settings.color_scheme }}">
  <div class="iwt__container">
    <div class="iwt__image">
      {% if section.settings.image != blank %}
        <img src="{{ section.settings.image | image_url: width: 800 }}" alt="{{ section.settings.heading }}" loading="lazy">
      {% endif %}
    </div>
    <div class="iwt__text">
      <h2 class="iwt__heading">{{ section.settings.heading }}</h2>
      <p class="iwt__body">{{ section.settings.body }}</p>
      {% if section.settings.button_text != blank %}
        <a href="{{ section.settings.button_link }}" class="iwt__btn">{{ section.settings.button_text }}</a>
      {% endif %}
    </div>
  </div>
</section>

<style>
  .iwt{
    padding:30px 16px;
    background:var(--color-background, #fff);
  }
  .iwt__container{
    max-width:1200px;
    margin:0 auto;
  }
  .iwt__image img{
    width:100%;
    height:280px;
    object-fit:cover;
    border-radius:12px;
  }
  .iwt__heading{
    font-family:var(--font-heading-family, 'Playfair Display', serif);
    font-size:22px;
    color:var(--color-base-accent-1, #ed2d5e);
    margin:16px 0 8px;
  }
  .iwt__body{
    font-size:14px;
    color:rgba(var(--color-foreground-rgb, 107, 114, 128), 0.7);
    line-height:1.7;
  }
  .iwt__btn{
    display:inline-block;
    margin-top:14px;
    background:var(--color-button, #2B2D42);
    color:#fff;
    padding:12px 24px;
    border-radius:8px;
    text-decoration:none;
    font-weight:600;
    font-size:14px;
  }
  @media(min-width:750px){
    .iwt{padding:70px 20px}
    .iwt__container{
      display:flex;
      gap:50px;
      align-items:center;
    }
    .iwt__image,
    .iwt__text{flex:1}
    .iwt__image img{height:420px;border-radius:16px}
    .iwt__heading{font-size:30px;margin-bottom:16px}
    .iwt__body{font-size:16px}
  }
</style>

{% schema %}
{
  "name": "Image with Text",
  "class": "section-iwt",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "What Makes Rorosaur Different?"
    },
    {
      "type": "textarea",
      "id": "body",
      "label": "Body Text",
      "default": "While our inspiration is traditional, Our use of science and technology is futuristic. Instead of Preservatives to give you Fresh food whenever you need [Shelf Life of 12 Months]"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    }
  ],
  "presets": [
    {
      "name": "Image with Text"
    }
  ]
}
{% endschema %}
